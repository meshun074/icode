package com.fdmgroup.icode.eshopping.repositories;

import org.springframework.stereotype.Repository;

@Repository
public class ProductRepository {

	/*
	 * Declare an List of Products
	 */

	public ProductRepository() {
		super();

		/*
		 * Set the list of Products to a starter set of 
		 * products by calling the sampleProducts method
		 */
	}
	
	/*
	 * Create a method that is private called sampleProducts() which
	 * will create an ArrayList of products and return that as a List
	 */

	/*
	 * Create a getter method for the List of 
	 * products stored in the repository
	 */


	/*
	 * Create a method getProductById()
	 * This method should take in a int and return an Optional of Product
	 */

	/*
	 * Create a method called addProduct()
	 * this will add a Product to the list of 
	 * products stored in this class
	 */
	
	/*
	 * 
	 * updateProduct()
	 * 
	 * Create a method to update Products
	 * This will take in a Product to update and 
	 * replace the previously stored products information 
	 * with the current its updated information
	 */

	/*
	 * deleteProductById()
	 * Create a method that deletes Products from the List
	 * 
	 */

}
